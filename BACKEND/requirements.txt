# FWA Risk Investigator — backend runtime dependencies
#
# NOTE: scikit-learn is pinned to 1.6.1 because the saved hybrid model artifacts
# (BACKEND/models/*.pkl) were pickled with scikit-learn 1.6.1. Loading them with a
# different minor version can silently change estimator behavior.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-multipart>=0.0.9

# Data + ML runtime (must match the training environment of the artifacts)
pandas>=2.2,<3.0
numpy>=1.26,<2.0
scikit-learn==1.6.1
xgboost>=2.1,<4.0
lightgbm>=4.3,<5.0
joblib>=1.3,<2.0
